[mutmut]
# Paths to mutate
paths_to_mutate = utils/,china_data_downloader.py,china_data_processor.py

# Test runner command with optimizations
runner = pytest -x

# Test directory
tests_dir = tests/

# Backup original files before mutation
backup = false

# Enable incremental mode for faster re-runs
# use_patch_file = true  # Commented out to avoid conflict with use_coverage

# Enable parallel execution (adjust based on CPU cores)
processes = 4

# Exclude patterns
exclude_patterns =
    */__pycache__/*
    */test_*
    */.git/*
    */venv/*
    */.venv/*
    */.pytest_cache/*
    */.mypy_cache/*
    */htmlcov/*
    */build/*
    */dist/*

# Cache directory for mutation results
cache_dir = .mutmut-cache

# Timeout for each test run (in seconds)
timeout = 60

# Use coverage to only mutate covered lines
# use_coverage = true  # Commented out to avoid conflict

# Coverage file to use
# coverage_file = .coverage  # Commented out since not using coverage

# For CI environments, add: --rerun-all
